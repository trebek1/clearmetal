import React from 'react';

const Home = () => (
  <div id="instructionsContainer">
    <div><span className="instruction"> Select Containers to see containers that need to be shipped today </span></div>
    <div><span className="instruction"> Select Vessels to see vessels leaving today </span></div>
    <div><span className="instruction"> Select Plans to see plans for placing containers on ships </span></div>
    <div><span className="instruction"> Select Assign Containers to create a new plan for placing containers on ships </span></div>
  </div>
);

export default Home;
